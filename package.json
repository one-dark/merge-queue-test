{
  "name": "merge-queue-test",
  "version": "1.0.0",
  "description": "Testing merge queue behavior with multiple PRs",
  "license": "ISC",
  "scripts": {
    "test": "node --test **/*.test.js",
    "test:watch": "node --test --watch **/*.test.js"
  }
}
